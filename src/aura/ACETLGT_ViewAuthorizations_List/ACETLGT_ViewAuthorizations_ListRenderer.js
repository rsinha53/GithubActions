({
	/**rerender : function(component, helper) {
        this.superRerender(); 
        
        setTimeout(function(){ 
            window.lgtAutodoc.initAutodoc();
            
        }, 1);
        
	}**/
})