<aura:component controller="ManageActivitiesController" implements="lightning:isUrlAddressable" >
    
    <!-- WorkSpace -->
    <lightning:workspaceAPI aura:id="workspace" />
    <!-- Workspace -->
    
    <aura:handler name="init" value="{!this}" action="{!c.doInit}"/>
    
</aura:component>