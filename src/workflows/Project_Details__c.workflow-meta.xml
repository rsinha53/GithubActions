<?xml version="1.0" encoding="UTF-8"?>
<Workflow xmlns="http://soap.sforce.com/2006/04/metadata">
    <rules>
        <fullName>Update Project Start Date</fullName>
        <active>false</active>
        <formula>OR(ISCHANGED(CPM_Project_Start_Date__c),AND(ISNEW(),   Not ISNULL(CPM_Project_Start_Date__c)))</formula>
        <triggerType>onAllChanges</triggerType>
    </rules>
</Workflow>
